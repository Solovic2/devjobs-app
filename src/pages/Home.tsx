import Layout from "../components/Layout";
import SearchBar from "../components/SearchBar";

const Home = () => {
  return (
    <Layout>
      <SearchBar />
    </Layout>
  );
};
export default Home;
